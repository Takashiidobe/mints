[package]
name = "mints"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = { version = "1.0.100", default-features = false }
libc_alloc = "1.0.7"

[profile.release]
lto = true
codegen-units = 1
panic = "abort"
strip = true
